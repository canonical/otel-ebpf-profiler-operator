name: Pull Requests

on:
  pull_request:
    branches:
      - main
      - track/**

jobs:
  pull-request:
    name: PR
    # FIXME: point at v1
    uses: canonical/observability/.github/workflows/charm-pull-request.yaml@OPENG-3328
    secrets: inherit
    with:
      provider: machine
